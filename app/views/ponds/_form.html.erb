<%= form_for(@pond) do |f| %>
  <% if @pond.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@pond.errors.count, "error") %> prohibited this pond from being saved:
      </h2>
      <ul>
        <% @pond.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field"> <%= f.label :name %><br>
  <%= f.text_field :name %></div>

  <div class="field"><%= f.label :water_type, "Water Type" %><br>
  <%= f.text_field :water_type %></div>

  <div class="actions"><%= f.submit 'Submit', :class => 'btn btn-primary' %></div>

<% end %>